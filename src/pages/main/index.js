import Weeklydate from "@/components/main/Weeklydate";

const index = () => {
  return (
    <div
      className={`w-screen h-screen flex flex-col justify-start items-center px-[30px]`}
    >
      <Weeklydate />
    </div>
  );
};

export default index;
